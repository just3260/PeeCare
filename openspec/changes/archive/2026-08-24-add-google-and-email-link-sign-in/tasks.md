## 1. Web 登入與 callback

- [x] 1.1 [P] 先在 `src/features/auth/auth-provider.spec.ts` 建立 Firebase SDK mock，覆蓋「Google popup sign in」的成功、popup blocked／cancelled、provider collision、無額外scope與raw error遮蔽，再依「Firebase 原生登入範圍限於 Google popup 與 Email Link」及「AuthProvider 擴充但 session observer 保持唯一權威」把 `AuthProvider` 改為 `signInWithGoogle`、`sendEmailSignInLink`、`completeEmailSignInLink`、`signOut`；以 `npm run test:unit -- src/features/auth/auth-provider.spec.ts` 驗證只有既有Auth instance與observer lifecycle被使用。
- [x] 1.2 [P] 先在 `src/features/auth/email-link-flow.spec.ts` 寫出「Bounded pending Email Link state」的exact schema、1,800,000 ms TTL、future timestamp、invalid JSON／extra key、unsafe return path、storage failure與cleanup案例，再依「Email Link callback 使用有期限 pending record 與重新輸入 fallback」實作 `src/features/auth/email-link-flow.ts`；以該Vitest檔確認Email永不進入action URL且storage unavailable會回到重新輸入流程。
- [x] 1.3 在 `src/views/SignInView.spec.ts` 先覆蓋「Firebase-native production sign-in methods」、「Passwordless Email Link request」與「First-release identity boundary」，再更新 `src/views/SignInView.vue` 顯示Google與Email Link兩個入口、移除password／Apple／One Tap／linking控制、對new／existing identity顯示相同送信確認並對collision顯示中性原登入方式指引；以 `npm run test:unit -- src/views/SignInView.spec.ts` 驗證UI與provider呼叫契約。
- [x] 1.4 先在 `src/views/EmailLinkSignInView.spec.ts`、`src/router/index.spec.ts`與`src/router/auth-guard.spec.ts` 寫出「Public Email Link callback completion」的same-browser、cross-device重新輸入、invalid／expired／consumed link、safe `returnTo`及protected-content denial案例，再新增 `src/views/EmailLinkSignInView.vue` 與公開 `/auth/email-link` route；以三份Vitest確認callback完成前不發布session、成功後清除record並只導向allowlisted path。
- [x] 1.5 更新 `src/features/auth/auth-emulator.integration.spec.ts`，透過Authentication Emulator取得out-of-band Email Link並驗證new-user、existing-user、UID切換teardown與sign-out，且不再以password作為本change的登入驗收；以 `npm run test:firebase` 證明Email Link仍由Firebase observer驅動既有三態session。

## 2. Development Firebase readiness

- [x] 2.1 [P] 依「Development readiness 驗證 provider 與 Email Link configuration」先擴充 `firebase/development/preflight.spec.ts` 與environment相關tests，覆蓋primary provider必須為`google.com`、readiness provider list不得含`apple.com`／`phone`／`password`及invalid inventory的zero-mutation結果，再更新 `firebase/development/environment.mjs`、`firebase/development/environment.d.mts`、`firebase/development/preflight.mjs`與`firebase/development/preflight.d.mts`；以對應Vitest確認sanitized plan只描述核准provider。
- [x] 2.2 先在 `firebase/development/readiness.spec.ts` 建立「Deployed Auth and Firestore readiness」table tests，逐一覆蓋Google disabled、Email disabled、`passwordRequired`非false、duplicate Email enabled、authorized domain缺漏及既有index／Rules failures，再更新 `firebase/development/readiness-admin-adapter.mjs`、`firebase/development/readiness.mjs`與`firebase/development/readiness.d.mts`讀取Identity Toolkit project config並回傳`auth_provider_not_ready`、`email_link_not_ready`或`authorized_domain_not_ready`；以該Vitest及secret scan確認summary不含Email、secret、token、OOB code或link。
- [x] 2.3 更新 `firebase/development/README.md`，讓operator能設定Google provider、Email enabled／password optional、duplicate Email denied與Hosting／action authorized domains，並明記backend仍可接受既有password credential但Web不提供password入口；以文件中的exact env名稱對照environment parser tests及執行 `npm run firebase:development:preflight -- --dry-run` 驗證指令未漂移。

## 3. Beta release authentication migration

- [x] 3.1 [P] 先在 `deploy/development/release-web-beta.spec.ts` 擴充「Non-PII beta tester inventory」案例，使schema、example與local inventory拒絕Email Link、OOB code及既有PII／credential patterns，再更新beta inventory validation與secret scan；以該Vitest確認0／1／2 tester boundaries不變且任何敏感值都在build或browser前回`inventory_invalid`。
- [x] 3.2 依「Beta live journey 以 hidden Email Link 取代 password」先為「Ephemeral tester credential handling」建立fake TTY tests，覆蓋exactly one Email＋one link、no echo、missing TTY、password／argument／environment／file／JSON rejection、HTTPS／approved domain／`mode=signIn`／OOB code validation及finally清空mutable references，再修改 `deploy/development/release-web-beta.mjs`；以focused Vitest確認failure分別為`credential_input_unavailable`、`email_link_invalid`或`tester_authentication_failed`且輸出通過secret scan。
- [x] 3.3 先為「Isolated beta tester journeys」的authentication half建立fake browser lifecycle tests，再讓runner在fresh context提交hosted Email、接受operator貼入的one-time link、完成`/auth/email-link`並確認signed-in UID exact匹配assigned device；以focused Vitest確認callback失敗與UID mismatch都會在任何Member API mutation前停止並清除Email、link、OOB code及token references。
- [x] 3.4 延伸「Isolated beta tester journeys」fake browser tests，使Email Link成功後續跑overview／history／stats／rename-clear／protected reload／sign-out與finally teardown；以focused Vitest確認成功及下游失敗都會清除Auth persistence、IndexedDB、Cache Storage、service-worker state及所有mutable authentication references。
- [x] 3.5 先建立「Hosted Google sign-in release boundary」tests，再讓beta verification確認exact hosted build含Google control、readiness record含enabled `google.com`且provider adapter gate已通過，同時讓healthy evidence明確不宣稱external Google account E2E；以 `deploy/development/release-web-beta.spec.ts` 驗證缺少control或readiness會阻止healthy record且不要求Google credential。
- [x] 3.6 依「Provider linking 與 Apple 明確排除」更新 `deploy/development/BETA_RELEASE_RUNBOOK.md`，將operator步驟從Email／password改為Email request＋mailbox copy＋hidden link paste，明記不使用mailbox API、Apple、One Tap、provider linking或account merge，並以runbook content test或人工content review確認沒有password prompt、secret-bearing command argument或不實Google E2E聲明。

## 4. 整體驗證與 release handoff

- [x] 4.1 依序執行 `npm run check`、`npm run test:firebase`、`npm run check:release`與 `npm run web:development:beta:dry-run`，確認Web、Auth Emulator、readiness、beta secret scan與dry-run全部通過；若未取得實際development provider及mailbox證據，handoff只能記錄local gates，不得宣稱Google或Email delivery已通過live acceptance。
