## 1. 測試先行固定契約

- [x] 1.1 先為 `Secure macOS runtime floor and native execution` 與設計決策 `macOS 14.8.8 is the runtime floor` 在 `scripts/test-tool-operator.spec.ts` 建立失敗測試，覆蓋 14.8.7／14.8.8／14.8.9／15.0.0、malformed `sw_vers`、arm64／x64 native 判斷及 Rosetta 拒絕；以 `npm run test:unit -- scripts/test-tool-operator.spec.ts` 確認測試只因尚未實作的 preflight contract 失敗。
- [x] 1.2 先為 `Personal gcloud identity and in-memory secret acquisition`、`Exclusive credential provider selection` 與 `Local server-side development secret boundary` 在 `scripts/test-tool-operator.spec.ts` 建立失敗 policy table，固定 argument exclusivity、無 shell spawn、單一 personal identity、project `petcare-c7483`、service-account／key-file／Emulator拒絕、numeric version、bounded secret、clear-on-all-exits 及 stdout/stderr redaction；以 focused Vitest 確認每個拒絕案例的 gcloud／Secret Manager／listen call count 為零。
- [x] 1.3 先為 `Safe loopback and browser lifecycle` 在 `scripts/test-tool-operator.spec.ts` 建立失敗狀態測試，固定 5055、一次 ephemeral fallback、explicit-port failure、`/usr/bin/open` no-shell invocation、`browser_open_failed` 非致命結果及 SIGINT／SIGTERM cleanup；以 focused Vitest 確認 safe URL 以外的 identity／credential data 不出現在 terminal events。
- [x] 1.4 [P] 先為 `Architecture-specific self-contained macOS artifacts` 與 `Deterministic pinned SEA build` 在 `scripts/test-tool-macos-build.spec.ts` 建立失敗 fixture tests，固定 manifest schema、Node/archive/injector hashes、三個 asset keys、snapshot/code-cache disabled、single-architecture output、wrong hash／universal／secret-like asset拒絕及 source/SEA byte parity；以 `npm run test:unit -- scripts/test-tool-macos-build.spec.ts` 確認 contract 尚未實作時失敗。

## 2. Operator runtime 與安全邊界

- [x] 2.1 在 `scripts/test-tool-operator.mjs` 實作 `macOS 14.8.8 is the runtime floor` 的版本／native architecture preflight與 exact CLI parser，讓 development-cloud預設、local profile、secret-version／secret-file exclusivity、port及 no-open 行為符合穩定 error codes；以 1.1、1.2 focused tests 全數通過並確認任何 prerequisite failure 都先於 gcloud與listen。
- [x] 2.2 在 `scripts/test-tool-operator.mjs` 與 `scripts/test-tool.mjs` 實作 `GcloudSecretProvider keeps secret in memory`：固定 gcloud operations、personal identity/project validation、numeric Secret Manager access、single-consumer secret holder、explicit cleanup及 owner-only file fallback，讓 browser/server只在 approved event request取得 credential；以 1.2 tests、`scripts/test-tool-server.spec.ts` 及 repository secret scan 驗證 raw gcloud output、account、token與secret零輸出。
- [x] 2.3 在 `scripts/test-tool-operator.mjs` 實作 `Loopback launcher owns port and browser lifecycle`：只綁定 `127.0.0.1`、5055 address-in-use後恰好一次 ephemeral retry、bound-address URL、absolute no-shell browser open與 signal/server cleanup；以 1.3 tests及一個 no-open process smoke證明沒有非loopback listener或殘留process。
- [x] 2.4 在 `scripts/test-tool.mjs` 實作 `Embedded assets preserve source execution` 的單一 asset-provider seam，讓 source mode讀取相鄰檔案、SEA mode透過 `node:sea` 讀取三個 immutable assets且HTTP routes/content types不變；以 `npm run test:unit -- scripts/test-tool.spec.ts scripts/test-tool-server.spec.ts scripts/test-tool-macos-build.spec.ts` 驗證既有行為及無相鄰檔案 fixture。
- [x] 2.5 在 `scripts/test-tool-operator.spec.ts` 先為 `Secure macOS runtime floor and native execution` 新增二段 `sw_vers` RED cases，再於 `scripts/test-tool-operator.mjs` 將恰好二段純數字版本補零為 patch component、保留三段版本並拒絕一段／四段／非數字輸出，使 `14.8` 仍為 `unsupported_macos`、`15.0`／`26.4` 可在任何 gcloud 或 listen 前通過版本 gate；以 `npm run test:unit -- scripts/test-tool-operator.spec.ts` 驗證所有版本 boundary 與 prerequisite call count。

## 3. 可重現雙架構 build 與發布證據

- [x] 3.1 在 `scripts/test-tool-macos-build.json` 與 `scripts/test-tool-macos-build.mjs` 實作 pinned manifest parser及輸入取得邊界，固定 Node.js 22 arm64/x64 archive URL與SHA-256、resource injector version/integrity、macOS 14.8.8、asset inventory及output names，拒絕redirect host、mutable／mismatched input與stale staging reuse；以 1.4 manifest／hash tests通過驗證。
- [x] 3.2 在 `scripts/test-tool-macos-build.mjs` 與 `.gitignore` 實作設計決策 `Two independently signed SEA artifacts` 的 arm64 staging path，先以 `package-lock.json` 精確鎖定的 esbuild 將repository ESM sources產生Node.js 22.23.2相容、無source map及無filesystem-relative runtime dependency的單一CommonJS SEA entry，再停用snapshot/code cache、注入三個assets、排除staging／release輸出並驗證單一arm64 Mach-O；以 build fixture、bundle inspection、`file`/Mach-O inspection、Git untracked-output assertion、真實artifact無相鄰assets或JavaScript modules啟動及privacy scan證明產物可進入signing但尚不可發布。
- [x] 3.3 擴充相同 build contract產生獨立x64 staging artifact，明確禁止 `lipo`／universal output並將x64標記為 qualification-pending；以 x64 manifest fixture、native Intel smoke入口與跨架構negative inspection證明arm64 artifact不能冒充x64且Rosetta evidence不能解除pending。
- [x] 3.4 在 `scripts/test-tool-macos-verify.mjs` 實作 `Signed notarized and privacy-safe release evidence` 與設計決策 `Release gate binds signing notarization and real-architecture qualification`：驗證Developer ID Application、Hardened Runtime、secure timestamp、notary acceptance、Gatekeeper、executable SHA-256及固定sanitized record schema，並要求兩個records具相同source/manifest hash；以 unsigned／ad-hoc／unnotarized／privacy-positive／single-record fixtures全部non-zero及valid paired fixture通過驗證。

## 4. Release gate 與 handoff

- [x] 4.1 在 `package.json`、`package-lock.json`、`scripts/check-release.mjs`、`scripts/check-release.spec.ts` 與 `scripts/TEST_TOOL_MACOS_RUNBOOK.md` 接入 `test-tool:macos:build`、`test-tool:macos:verify`、`test-tool:macos:release` 及operator handoff，讓快速release gate驗證manifest、tests、audit與privacy而不需要live signing/cloud mutation，runbook則精確記錄gcloud登入、numeric version、checksum、架構選擇、啟停、撤權與withdraw流程；以 `npm run check:release` 及runbook content assertions驗證。
- [x] 4.2 先為 `Architecture-specific macOS runtime floors` 與 `Secure macOS runtime floor and native execution` 在 `scripts/test-tool-operator.spec.ts`、`scripts/test-tool-macos-build.spec.ts` 及 `scripts/test-tool-macos-verify.spec.ts` 建立RED tests，固定arm64 floor `14.8.8`、x64 floor `14.6.0`、拒絕root-level fallback、SEA architecture/floor配對及release-record配對；boundary table必須覆蓋arm64 `14.8.7`拒絕／`14.8.8`接受、x64 `14.5.9`拒絕／`14.6`與`14.6.0`接受，並以focused Vitest確認測試只因現行shared floor與hard-coded verifier失敗。
- [x] 4.3 在 `scripts/test-tool-macos-build.json`、`scripts/test-tool-macos-build.mjs`、`scripts/test-tool-operator.mjs`、`scripts/test-tool-macos-verify.mjs` 與 `scripts/TEST_TOOL_MACOS_RUNBOOK.md` 實作架構別runtime floor：build manifest把`minimumMacOS`放入兩個architecture entries、SEA只嵌入所選architecture/floor、runtime在任何gcloud或listen前比較embedded floor、release verifier要求record floor等於相同architecture manifest entry；以4.2 focused tests、`npm run test:unit -- scripts/test-tool-operator.spec.ts scripts/test-tool-macos-build.spec.ts scripts/test-tool-macos-verify.spec.ts`及`npm run check:release`驗證，且manifest hash仍完整涵蓋兩個architecture entries。
