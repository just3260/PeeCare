## 0. 本機 development-cloud bridge（第一階段）

- [x] 0.1 實作「Local development-cloud bridge remains loopback-only」、「Loopback development-cloud bridge」與「Local server-side development secret boundary」：在 `scripts/test-tool.mjs` 建立 fail-closed `local|development-cloud` profile、sanitized config endpoint、exact HTTPS origin/path/method allowlist及operator-only secret-file injection，維持`127.0.0.1` listener並在browser Authorization、live Firestore或任意remote proxy進入fetch前拒絕；新增不含secret值的 `scripts/test-tool.development.env.example` 與 `scripts/test-tool-server.spec.ts`，以startup matrix、zero-fetch denial、secret absence、approved health/event forwarding及local regression驗證。
- [x] 0.2 實作「Hosted Web observation handoff」：在 `scripts/test-tool.html`／`scripts/test-tool.spec.ts` 依sanitized profile顯示development banner與固定`https://petcare-c7483.web.app`開啟控制，cloud profile移除secret DOM值並停用device registry/custom-name live操作，同時保留local profile既有UI與15項測試；以DOM tests、repository secret scan及marked development device的manual urination/battery-to-Hosting projection smoke驗證。

## 1. Test Tool API 基線與授權

- [x] 1.1 [P] 建立獨立 `services/test-tool-api` package、Fastify health/server/config與 non-root container，實作「Exact-origin browser and disable boundaries」：只允許 exact development Web CORS、8 KiB JSON boundary、`PEECARE_TEST_TOOL_ENABLED=true`及 approved project/origins/secret file，invalid config在 listen/Firebase前失敗；以 config/server/app tests、foreign-origin table、disable switch及 Docker non-root health smoke驗證。
- [x] 1.2 [P] 實作 revoked-aware Firebase ID token verifier，missing/malformed/expired/revoked/wrong-project token一致回 `401 unauthorized`且不呼叫 repository；以 token adapter與 app call-count tests驗證不記錄 token、email或 raw UID。
- [x] 1.3 實作「Owner and development marker are rechecked server-side」與「Server-side tester and device authorization」：Firestore parser/list/repository只接受 matching Owner、enabled ingestion、valid product model及 exact `developmentTestTool` marker，missing/foreign/disabled/malformed/unmarked皆相同 `404 test_device_not_found`且 zero usage/ingestion call；以 repository unit及 Emulator authorization matrix驗證 registry-preserving behavior。

## 2. Typed event、ledger 與 ingestion boundary

- [x] 2.1 [P] 實作「Dedicated API exposes typed operations, never generic proxy」與「Typed event API without generic proxy capability」：exact urination/battery body validators拒絕 extra fields、caller URL/header/topic/project/product/event identity/transport control、wrong media type及 oversized body；以 boundary tables與 generic-proxy negative tests驗證所有拒絕路徑 zero repository/ingestion call。
- [x] 2.2 實作「Firestore usage ledger enforces bounded development rate」與「Transactional tester rate and sequence ledger」：同一 transaction完成 owner/marker read及 hashed-UID ledger reservation，強制1000ms間隔、500/UTC-day quota、day reset、uint32 sequence exhaustion與 concurrent single winner；以 fake clock boundary、499/500、999/1000ms、max sequence、upstream-failure reservation及 Emulator race tests驗證。
- [x] 2.3 實作「Server generates canonical event identity and forwards with mounted secret」與「Server-generated canonical event envelope」：registry product model、reserved sequence、UUID eventId、server timestamps、fixed firmware/client/username/QoS/retained及 event-specific topic組成 canonical envelope，ingestion client只讀 mounted secret並只呼叫 exact HTTPS event path；以 golden urination/battery envelopes、unique identity、upstream status mapping與 unsafe-origin/secret-file tests驗證。
- [x] 2.4 組合 device list與 event endpoints並落實「Server-side ingestion secret and privacy boundary」：success只回 allowlisted device/result fields，stable 400/401/404/409/413/415/429/500/503 responses含 request ID，structured logs/ledger/release output拒絕 secret、Authorization、identity、customName、body、payload及 full upstream response；以 complete app matrix、response schema、log redaction fixtures與 source/bundle secret scan驗證。

## 3. Development Web route 與 cache safety

- [x] 3.1 [P] 實作「Verified Test Tool API origin handoff」：Web config與唯一 `test-tool-api` adapter只接受 healthy same-project immutable service record的 exact HTTPS origin，每次 request從既有 Firebase user取得 ID token並驗證 canonical response，missing/loopback/HTTP/path/wrong-project/stale record在 Hosting upload前失敗；以 config/adapter table tests及 Web build dry-run驗證。
- [x] 3.2 實作「Test tool lives behind existing member auth as a protected web route」、「Authenticated tester event tool access」與「Protected development test-tool route」：新增 `/test-tool` route及 Vue UI，沿用 auth lifecycle、只顯示 API eligible devices、提供 validated urination/battery forms與 sending/success/error states，不新增 sign-in store、generic settings或產品主導航入口；以 route guard、direct reload、signed-out leak、empty/eligible devices、form keyboard與 API call tests驗證。
- [x] 3.3 實作「Test-tool member data cache exclusion」：service worker與 Web lifecycle不cache Test Tool API/device/result/form data，sign-out清除頁面 state，offline reload只顯示 sign-in shell；以 Cache Storage/network inspection、prior marker scan、sign-out/offline browser test及 PWA build assertions驗證。

## 4. Integration、deployment 與 live handoff

- [x] 4.1 將 Test Tool API的 Firestore Emulator integration納入 `scripts/test-firebase.mjs`、root `check`與 `check:release`，覆蓋 token/Owner/marker、client direct usage-ledger denial、concurrent quota reservation及 event-to-projection，並以 `npm run check:release`證明 local tool既有15 tests與所有 workspaces同時通過。
- [x] 4.2 實作「Independent immutable Cloud Run deployment and Web handoff」與「Immutable development Test Tool API deployment」的 manifest/deploy/rollback：digest-only image、dedicated identity、numeric secret version、min0/max2/concurrency20、budget/project/region gates與 sanitized release record，wrong target/tag/identity/secret在任何 cloud mutation前失敗；以 deploy plan snapshots、gcloud adapter spies、Docker smoke與 rollback ambiguity tests驗證。
- [x] 4.3 完成 live verifier、Web handoff與 runbook：先驗 health/CORS/401 zero-write/foreign/unmarked/rate limit，再由 operator標記 beta device並驗證 urination/battery經 Ingestion寫入 Firestore且 Web可見，healthy record才提供 `VITE_TEST_TOOL_API_URL`；以 deploy/verify dry-run、live sanitized smoke、Hosting `/test-tool` reload、secret/PII scan及 reviewed rollback/disable sequence完成交付。
